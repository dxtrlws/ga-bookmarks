<h1>Bookmarks#index</h1>
<p>Find me in app/views/bookmarks/index.html.</p>
<%= link_to("Add Bookmark", new_bookmark_path) %>

<ul>
  <% @bookmarks.each do |bookmark| %>
    <li><%= link_to("#{bookmark.description}", bookmark.url ) %> <%= link_to("Edit", edit_bookmark_path(bookmark)) %> <%= link_to("Show", bookmark_path(bookmark))%></li>
  <% end %>

</ul>